<h1>My Tasks</h1>

<% @tasks.each do |task| %>
  <h3><%= link_to task.title.capitalize, task_path(task) %></h3>
  <input type="checkbox" name="completed" <%= task.completed ? "checked" : "" %> >
<% end %>

</br>

<%= link_to "Add a new task", new_task_path %>
